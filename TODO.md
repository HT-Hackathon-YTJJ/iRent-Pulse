# TODO

Demo 前還沒做完的事。做完就把項目劃掉並註明日期，不要刪除——哪些事情做過、
為什麼那樣決定，是這份文件比 issue tracker 有用的地方。

---

## 1. Demo 素材：七個 Slot 的照片

學姐會提供素材。收到後要對照下表逐一檢查，缺的補拍。

| # | Slot（`CaptureSpot`） | 送到後端的 `slot` | L1 走哪個 prompt | 需要的素材 |
|---|---|---|---|---|
| 1 | `fuelCard` | `加油卡/停車卡` | 卡片（第 3 項新增） | 遮陽板上的卡套（兩格插袋都要入鏡） |
| 2 | `interiorFront` | `前座` | 車內整潔 | 前座 |
| 3 | `interiorRear` | `後座` | 車內整潔 | 後座 |
| 4 | `frontLeft` | `左前` | 車外車損 | 停在路邊的實景照 |
| 5 | `frontRight` | `右前` | 車外車損 | 停在路邊的實景照 |
| 6 | `rearLeft` | `左後` | 車外車損 | 停在路邊的實景照 |
| 7 | `rearRight` | `右後` | 車外車損 | 停在路邊的實景照 |

- [x] **2026-09-08** 收齊七個 slot 的乾淨版素材，放在 `demo/return_photos/`
- [x] **2026-09-08** 情境 a：`demo/return_photos/後座有垃圾.webp`
- [x] **2026-09-08** 情境 b：`demo/return_photos/左後被撞擊.webp`（左後保險桿凹陷）

素材以 **WebP q95** 進版控（PSNR ≈ 43 dB，17 MB → 3.8 MB）；原始 PNG/JPEG 留在
本機、`.gitignore` 排除。畫質沒有損失可講：`openrouter.encode_image` 本來就會把
每張圖縮到長邊 1024 再轉 JPEG q85，原檔多出來的位元一格也到不了模型面前。

七張乾淨版共用於兩組情境——情境 a 只換掉後座、情境 b 只換掉左後，其餘六張相同，
這就是上面那段「要兩組各七張」要的東西。

**唯一一張要留意的是 `demo/return_photos/前座.webp`，只有 478×319。** 它在 10 次預檢裡都判為可判讀
（見下面第 4 項），所以不擋 demo，但它是全部素材裡唯一一張沒有餘裕的。學姐 Drive 的
`real_indoor_front.png` 是 1440×1440，只是換了一台車（棗紅內裝、車牌 ABC-123），
混進來會讓同一趟還車的七張照片不是同一台車，所以沒有換。要換的話七張要一起換。

**要兩組各七張，不是兩張。** L1 是逐張獨立判斷的，情境 a 的那一組除了後座
以外都要是乾淨的，情境 b 的那一組除了受損的那一角以外都要是完好的。只換一張、
其他 slot 隨便拍，判定會被其他照片的誤判污染。

生成受損照片時，讓損傷**明顯**：凹陷、鈑件變形、大片刮痕。`api/app/prompts.py`
的 `_NOT_DAMAGE` 明列水漬、反光、灰塵、貼紙一律不算損傷，模糊地帶的細紋正是
判定會左右搖擺的來源。

---

## 2. 翻拍螢幕會不會被 L1 擋下來（**素材到齊後第一件要測的事**）

2026-09-07 用 Pixel 10 Pro 實測，隔著螢幕拍一張**照片級真實**的車照送 L1：

```json
"assessable": false,
"assessable_reason": "照片嚴重模糊失焦且為翻拍螢幕畫面，無法看清車身表面漆面細節。"
```

模型**點名了「翻拍螢幕畫面」**。但同一句話也說了「嚴重模糊失焦」，而那張確實模糊
（手機架著、對焦沒收斂），所以兩個因素分不開。

對照組（同樣的圖，用檔案直接上傳、不經過翻拍）兩張都過不了，但理由都不是翻拍：

| 圖 | L1 的理由 |
|---|---|
| 原始整車照 | 拍攝距離過遠，車體佔比小於三分之一 |
| 裁切近拍（4× 放大） | 解析度過低且模糊 |

兩個判斷都對——那兩張圖本來就有這些問題。所以目前**還不能斷定**翻拍本身會被擋。

- [x] **2026-09-08** (a) 半邊做完了：`api/scripts/preflight.py` 跑 `demo/return_photos/` 九張、
      每張 10 次，**全部 10/10 通過**（詳見第 4 項）。上面兩個對照組被擋掉的理由
      是「車太小」與「太模糊」，新素材兩個都沒有——所以那兩次失敗確定是素材品質，
      跟翻拍無關
- [ ] (b) 半邊還沒跑，因為要有人真的拿手機對著螢幕拍。工具已經備好：
      把翻拍的七張用**一樣的檔名**放進一個資料夾，然後
      `api/.venv/bin/python api/scripts/preflight.py --compare demo/rephoto`，
      它會印出 (a)/(b) 並排，並在 (a) 過而 (b) 不過的那幾張標出來
- [ ] 若確認是問題：在 `api/app/prompts.py` 加 `DEMO_MODE` 環境變數，開啟時補一段
      「照片可能翻拍自螢幕或列印品，摩爾紋／畫面網格／紙張紋理與折痕不算損傷，
      也不因此判定為不可判讀」。**這是要告訴模型忽略它正確觀察到的事實，是個
      demo 取捨，做之前先確認大家同意**
- [ ] 或者印出來拍（A4 霧面雷射）：沒有摩爾紋、沒有更新率條紋、反光低得多，
      而且不用改 prompt
- [ ] 不論走哪條路，素材本身要夠大夠清楚：上面兩個對照組是被「車太小」和
      「太模糊」擋掉的，這跟翻拍無關，是素材品質

---

## 3. `加油卡/停車卡` 走錯 prompt（**擋路中，先解**）

`INTERIOR_SLOTS = {車內, 後座, 前座, 駕駛座, 副駕}`（`api/app/main.py`）沒有
`加油卡/停車卡`，所以第一個 slot 的遮陽板特寫會送進「判斷車身漆面」的 prompt，
而且 `angle_verified` 的 enum（`models.py` 的 `ANGLES`）根本沒有這個值。幾乎
必定回 `assessable=false`（「根本沒有拍到車」），後果同上一項。

- [x] **2026-09-08** 走第三種 prompt。加進 `INTERIOR_SLOTS` 會讓 L1 去問一張塑膠卡
      的「車內整潔度」，一樣答非所問；跳過 L1 則是把第一個 slot 變成沒有人在看的
      slot。新的 `SlotKind.card` 走 `prompts.L1_CARD_SYSTEM`，問的是三件事：卡片
      看不看得清楚、卡片還在不在車上、**卡片上印的車牌是什麼**。
      `main.slot_kind()` 依 slot 字串分流，`models.SlotKind` 是那三種問題的名字。

      問的是「兩格插袋裡各自還有沒有卡」——`parking_card_present` 與
      `fuel_card_present` 分開回答，因為一格空、一格有卡是常見情形。
      實測 `demo/return_photos/加油卡和停車卡.webp` 3/3 都答對（停車卡 False、
      加油卡 True），沒有把印在卡套上的「停車卡」字樣當成卡片。

      **缺卡不會要求重拍。** 照片本身清清楚楚，重拍一個空插袋不會讓卡片回來，
      所以 `retake_required` 維持 false、可判讀性那條 bar 維持綠色，缺卡改用
      分析頁下方的問題卡片呈現。

      2026-09-08 稍晚：原本這一版還讓 L1 讀卡片上印的車牌並比對，
      後來確認要辨識的是**卡套**不是卡片正面——卡套照片裡看不到車牌——
      所以那段連同 `app/plate.py` 一起移除了。

---

## 4. 判定一致性

目標是同一張圖每次都給一樣的結果。按影響大小排，模型本身的隨機性其實是最小的那個。

- [x] **2026-09-08** `openrouter.py` 鎖 provider 與 seed。`config.L1_PROVIDER`
      預設 `google-vertex`（用 OpenRouter 的 `/models/{id}/endpoints` 確認過這個
      slug 對 `google/gemini-3.7-flash` 有效），`allow_fallbacks: false`，
      `VLM_SEED=20260908`。設成空字串就解除 pin——供應商掛掉時要留這條路，因為
      `allow_fallbacks: false` 的意思就是寧可整個失敗也不要偷偷換一個模型
- [ ] L1 自我一致性投票：同一張圖併發跑三次取多數決。**目前看起來不需要**——
      九張素材每張 10 次都同判，投票要解決的抖動沒有出現。素材換了要重跑預檢再決定
- [x] **2026-09-08** `api/scripts/preflight.py`。它問兩個不同的問題：
      **穩定**（每次判定一不一樣）與**正確**（那個判定是不是 demo 要的），
      兩個都過才算數——一張乾淨的車被穩定地判成有損傷，是穩定而無用。
      判定不一致或不符預期就 exit 1，可以直接當 demo 前的關卡。

      2026-09-08 實跑 `--runs 10`（90 次呼叫、$0.26）：

      | 照片 | 一致 | 判定 |
      |---|---|---|
      | 加油卡 | 10/10 | 卡片在、車牌 `REN-0000` 相符 |
      | 前座／後座 | 10/10 | 乾淨 |
      | 後座有垃圾 | 10/10 | 髒汙 |
      | 左前／右前／右後／左後 | 10/10 | 無損傷 |
      | 左後被撞擊 | 10/10 | 行李廂蓋＋後保險桿中央凹陷 moderate，升 L2 |

---

## 5. 車牌辨識：實機已驗，剩下的

2026-09-07 在 Pixel 10 Pro 上跑通了，隔著螢幕翻拍也讀得到：

```
L0 車牌: 702x608 ocr=REN-0000 候選=[REN0000] → match
L0 車牌: 720x600 ocr=ABC-1234 候選=[A8C1234] → mismatch
```

相符時維持綠框；不符時輪廓線變紅、badge 顯示「車輛不符」、提示「這不是你租的車」，
按下快門跳出可覆寫的面板。裁切約 700×610、約 1.5 Hz、沒有跟偵測器搶到卡頓。

`--dart-define=L0_LOG_PLATES=true` 可以把每次辨識結果印到 logcat（預設關閉）。

- [x] **2026-09-08** 加油卡/停車卡 那個 slot 也開了車牌辨識。卡片正面就印著車牌，
      而且是一張手臂距離、光線均勻的平面印刷品——比十公尺外的保險桿好讀得多，
      所以這個 slot 其實是全流程最可靠的身分驗證，不是最弱的。
      `CaptureSpot.readsPlate` 取代原本用 `isCorner` 當代理的判斷；四個角仍然裁
      偵測器給的車體框，卡片沒有車可偵測，改裁輪廓線的框。判定與文案沿用
      `AimState.wrongCar`／「這不是你租的車」——卡片是別台車的，人就是坐錯車了。
      伺服器端 L1 也讀同一張卡（第 3 項），兩邊用同一張混淆表
- [ ] iOS 完全沒驗過。部署目標已提到 15.5，`pod install` 要重跑，BGRA8888 那條
      路徑一行都沒在真機上跑過
- [ ] 站遠一點還讀不讀得到？目前只在「車佔滿畫面」的距離測過
- [ ] 真實車牌的字體與台灣車牌不同（測試用的是 Arial Bold），實車素材到了要重測。
      **`demo/return_photos/` 的七張都是真實台灣車牌 `REN-0000`**，可以拿來翻拍重測；L1 伺服器端
      已經在這批素材上 10/10 讀對，端上 ML Kit 那條路還沒用它們驗過

---

## 5.5 分析頁改版（2026-09-08）與**還沒部署的後端**

拍照流程與分析頁在 2026-09-08 大改了一輪，見下面「已完成」。其中一項有先後順序：

- [x] **2026-09-08** `api/` 已重新部署到 Fly（`tool/deploy_fly.sh`）。在那之前
      線上跑的還是 9/07 的程式碼——沒有 `SlotKind.card`、沒有卡套 prompt，
      第一個 slot 會掉進「判斷車身漆面」並要求重拍。

      部署後直接對 `https://irent-pulse-api.fly.dev/v1/l1/screen` 送
      `demo/return_photos/加油卡和停車卡.webp` 驗過：`assessable=true`、
      `retake_required=false`、`停車卡=false`、`加油卡=true`。
      實機用預設網址（不經 `adb reverse`）也跑通了同一張。

      **本機要改 prompt 時**用 `adb reverse tcp:8000 tcp:8000` 加
      `--dart-define=API_BASE_URL=http://localhost:8000`，不用每次都推線上。

---

## 6. 發版：APK + IPA + GitHub Release

上面都確認可以運作之後才做。

- [x] **2026-09-08** `flutter build apk --release` 可以跑了——**在此之前它是壞的**。
      R8 在 release 才會跑，所以這個 bug 從來沒有人撞到過，而它有兩層：
      1. 建置直接失敗。`google_mlkit_text_recognition` 在同一個 `initialize`
         裡宣告了 ML Kit 支援的每一種文字（拉丁、中、日、韓、天城文），但每一種
         的模型是**各自獨立**的 Gradle 相依，這個 App 只拉了拉丁那一個。
      2. 修掉之後建置會過，但 App 起來會在 logcat 印
         `ComponentDiscovery: NoSuchMethodException: TextRegistrar.<init> []`
         ——**不會 crash，只是文字辨識器安靜地沒有註冊**，也就是每一個同仁裝到的
         版本車牌比對都是死的。ML Kit 是靠 manifest 裡的 `<meta-data>` 反射
         實體化 `ComponentRegistrar`，R8 看不到呼叫端就把無參數建構子拿掉了。

      規則放在 `android/app/proguard-rules.pro`。實機用 release build 驗過
      `L0 車牌: reader=就緒 expected=REN-0000`
- [x] **2026-09-08** 決定只附兩個：**arm64-v8a 50 MB**（近八年的手機都是這個，
      叫同仁拿這個）與 **universal 115 MB** 當 fallback。armeabi-v7a（41 MB）
      與 x86_64（54 MB）沒附——前者要十年前的手機才用得到，後者只有模擬器
- [ ] `flutter build ipa`（目前沒有 TestFlight，先產出檔案）。**建議先不要放進
      release**：第 5 項寫的 iOS 從來沒有在真機上跑過一行，BGRA8888 那條路徑
      也沒驗過，附一個沒人測過又裝不了的 IPA 只會讓人以為它能用
- [x] **2026-09-08** 開了 GitHub Release `v1.0.0`（只有 Android）：
      <https://github.com/HT-Hackathon-YTJJ/iRent-Pulse/releases/tag/v1.0.0>
- [x] **2026-09-08** Release note 寫了：裝哪一個、怎麼允許未知來源、要哪些權限、
      不需要連我們的 Wi-Fi、後端連不上會退回腳本情境，以及三個已知限制
      （沒有 iOS、debug key 簽章、Store 停機會清空）

發版前記得確認 `InspectionApi.deployedBaseUrl` 指向正確、且 Fly 上的 App 是醒著的。
2026-09-08 這次兩件都確認過：`api/` 重新部署後實機用預設網址跑通了整條路。

**下一版要記得**：`pubspec.yaml` 的 `version:` 還停在 `1.0.0+1`，發 v1.0.1 之前
要先往上加，不然兩個版本的 versionCode 一樣，Android 不會讓你蓋上去。

---

## 7. 部署後的雜項

- [ ] Fly 機器閒置會停機。Demo 前先打一次 `curl https://irent-pulse-api.fly.dev/healthz`
      叫醒它（喚醒約 1–2 秒，但別讓評審等那一下）
- [ ] `/v1/l1/screen` 目前沒有任何驗證，任何人拿到網址都能燒 OpenRouter 額度。
      去 OpenRouter 後台設一個支出上限；真要擋就加一個共用 header token
- [ ] 記憶體 Store 在機器停機時會清空。同一次 demo 內從拍照走到 finalize 沒問題
      （機器不會在那幾分鐘內停機），但隔天回頭看 `/v1/orders/{id}` 會是 404

---

## 已完成

- [x] **2026-09-07** 後端部署到 Fly.io（`irent-pulse-api.fly.dev`）。原訂的
      Hugging Face Spaces 走不通：HF 已改成 Docker Space 需要 PRO 訂閱，只有
      Static Space 免費
- [x] **2026-09-07** `InspectionApi` 預設指向部署好的網址，`/healthz` 探測
      timeout 從 3 秒放寬到 12 秒（Fly 冷啟）
- [x] **2026-09-07** L0 車牌辨識：四個角用 ML Kit 端上文字辨識比對車牌，不符時
      輪廓線變紅、提示「這不是你租的車」，但**不鎖快門**（仍可送出）。比對規則
      有測試，OCR 路徑待實機驗證，見上面第 5 項
- [x] **2026-09-07** 拍攝 slot 可點擊自由切換與重拍，拍完自動跳下一個未拍的（實機已驗）
- [x] **2026-09-08** 拍照還車的素材全面換成學姐給的 1440×1440 母檔。原本整條鏈的
      上游都是 72×72（設計 repo 的 `slot_paint_*.png`），所以 `gen_slot_assets.py`
      整支腳本都在講「怎麼把 72px 放大 28 倍還能看」。現在母檔放在
      `tool/slot_paint/`（無損 WebP，2 MB，不進 `assets/` 所以不會被打包進 App），
      輪廓線變成 1.4× 放大而不是 28×，strip 縮圖從 72×72 變成 288×288（4× tile，
      對得上 3.125 的螢幕）。`guideAspect` 七個值跟著母檔的裁切重量過

### 2026-09-08 拍照流程與分析頁改版

- [x] **加油卡/停車卡改成辨識「卡套」而不是卡片正面**。要確認的事情其實是
      「兩張卡還在不在車上」，那看的是遮陽板上的卡套，而卡套照片裡根本沒有車牌。
      端上那一格的車牌辨識連同伺服器端讀卡片車牌的 prompt、`app/plate.py`
      一起移除；`CaptureSpot.readsPlate` 回到用 `isCorner` 判斷
- [x] **加油卡/停車卡的匡線改用程式畫**（`_CardPouchPainter`）。這一格要的是一個
      版面——品牌面板加左右兩個插袋——不是某一個卡套長什麼樣子。三個圓角矩形
      加兩個標籤，向量畫的在任何尺寸都銳利，也不用進 bundle
- [x] **前座／後座不再畫輪廓線**。手機離椅背一個手掌寬，每個人的坐姿都不一樣，
      畫出來的輪廓沒有人對得上。strip 的 slot 縮圖仍然有示意圖
- [x] **三個車內 slot 拿到 `aimSlack = 0.35`**。門檻預設值是照「白天、手臂距離、
      一整台車」調的，車內是屋頂底下的深色近拍，那組模糊下限與曝光上限進不去。
      一個永遠綠不了的 slot 會讓人重試到放棄還車
- [x] **slot 的重拍按鈕換成整格琥珀色覆蓋 + 警告圖標**。原本右下角那個 ↻ 看起來
      像重拍的控制項，但它不是——回到某一格的方法是點那一格，七格都一樣、
      不需要自己的 affordance。覆蓋整格在 strip 對面就看得到，標的是**照片**
      而不是照片的一角。`failed`（沒人看過那張）維持灰色雲朵，不上琥珀：
      那不是使用者能修的事
- [x] **拍完的照片會縮小飛進 slot**（`_flyIntoSlot`，420ms）。原本快門與格子之間
      什麼都沒有：照片這一幀還不在畫面上、下一幀就成了四格之外的 72pt 縮圖，
      而 strip 同時也在滑動——兩件事一起動，誰也沒解釋誰
- [x] **分析頁一次列出所有問題**，`ReturnIssueScreen` 整頁移除。原本會挑一個問題
      跳到單獨的畫面要求重拍，所以兩張爛照片的人是一次知道一個、跑兩趟，
      而且不知道還有第二個。現在每個問題一張卡片：左邊是他自己拍的那張照片，
      右邊是 L1 自己的說法。**單欄全寬垂直堆疊**——內容本來就是橫的（縮圖＋一句話），
      346dp 分兩欄每張只剩約 160dp，放不下能看的縮圖加能讀的句子；
      三四張以上就只是往下捲，卡片設計完全不用變
- [x] **`_CheckCard` 改成流式排版**。原本是固定 113.778pt 高、子元素全部絕對定位
      （照 Figma frame 做的），「後座和右後不通過」在 346dp 上會折行，第二行就直接
      畫穿卡片底部。實測七張全部不通過時那行折成三行，卡片跟著長高
- [x] **分析頁的判定改成每次通知都重算**。`widget.analysis` 是流程切畫面那一刻的
      快照，而照片還在飛——七張拍完、五張回來時開頁，它就一直說「5 張照片皆可判讀」，
      另外兩張在後面默默落地。改成 `live?.analysis ?? widget.analysis`

### 2026-09-08 修掉的 bug

- [x] **strip 第一個（加油卡）與最後一個（左後）點不動，中間的可以**。
      `Transform.translate` 包在 `OverflowBox` **外面**：hit test 進到 `Transform`
      會先被反向平移，於是一個畫在畫面正中央、但整條 rail 已經右移 240pt 的 tile，
      到了 `OverflowBox` 手上變成畫面左邊 240pt 處的座標——落在它自己那個「永遠只有
      一個螢幕寬」的 box 外面，`RenderBox.hitTest` 的 `size.contains` 就先擋掉了，
      根本輪不到 tile。中間幾格位移小、平移後還落在螢幕內所以正常，兩端的鄰居是
      半格能點——那就是「有點 lag」的真相：點在死掉的那半邊，什麼都沒發生。
      修法是把平移移到 `OverflowBox` **裡面**、包住 rail 本身，rail 自己的 box 是
      七格 552pt，位移後仍在其中。
      **這個 bug 在預設 800×600 的 widget test 裡看不到**——800pt 放得下整條 rail，
      根本不需要滑動。新測試 `the strip walks end to end on a real phone-width
      screen` 把 view 設成 346×771（Pixel 10 Pro 放大顯示後的邏輯尺寸）才複現得出來
- [x] **strip 點擊卡頓**。`Image.file` 沒有 `cacheWidth`，每個 72pt 的 tile 都在解
      相機原始 JPEG（約 4000×3000，解成 ARGB 是 48 MB）。七張塞不進 100 MB 的
      image cache，於是每次 rebuild 就淘汰一張、再重解一張，全在 raster thread 上。
      點擊其實有進來，是它落在的那一幀畫了 200ms。tile 解到 512 寬、凍結的那張解到
      1440 寬
- [x] **拍完之後 slot 白框的四個角被照片蓋掉**。白框放在 `Container.decoration`，
      而 `decoration` 是畫在 child **後面**的；`BoxDecoration` 有 border 時又會把
      child 內縮 2pt，但裁切用的仍是外框那個圓角矩形——直邊上還看得到框線，四個角
      照片的方角就跑到框線的弧線外面把它蓋掉。改用 `foregroundDecoration` 畫在
      照片上層。順帶不會再有「選到某格時內容縮 2pt」的跳動

### 實機測試當天修掉的 bug

- [x] **`carNo` 寫死成 `RDS-6583`**，但實際車輛是 `REN-0000`。所有 L1 上傳都被歸
      到一台使用者沒看過的車；車牌比對上線後會變成 App 對著自己租出去的車喊
      「這不是你租的車」。改成預設取 `VehicleProfile.plate`
- [x] **後端可連線時相機開不起來**。`_probe()` 探到 `/healthz` 後呼叫
      `_restart(ReturnScenario.live)`，那會 `_captureRun++` 把拍攝畫面重掛；舊
      `CaptureSession.dispose()` 是非同步而 `State.dispose()` 不能 await，新的
      session 在舊的 CameraX use case 還沒交回時就綁，CameraX 拒絕，整個還車流程
      掉進「無法開啟相機」的 fallback。**這個 bug 只在部署之後才會出現**，本機沒
      跑後端時探測失敗反而不會觸發。修法：`_probe` 不再重掛畫面，另外 `start()`
      與 `dispose()` 都排進一個靜態佇列，確保交接完成才開新的
- [x] **車牌 mismatch 的連續計數改成按「同一個車牌」計**。實測鏡頭掃到一面文字時
      OCR 一次吐出 11 個「車牌形狀」的候選，每幀都不一樣；原本按「次數」計會在三
      幀後誤判紅框。真的站錯車時車牌每次都讀成同一串，所以照樣兩秒內觸發
- [x] **車牌檢查排到曝光／清晰度之前**。原本排在後面，昏暗環境下站錯車只會看到
      「光線不足」——叫人去調光線，而他根本站錯車
